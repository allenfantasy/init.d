username=`pwd`
sudo add-apt-repository ppa:pitti/postgresql
sudo apt-get update
sudo apt-get -y install postgresql-9.2 libpq-dev
su postgres --command "createuser -P -d -s dongxi"
#su postgres --command "createdb -O $username $username"
echo "PATH=\$PATH:/etc/init.d/postgres" >> ~/.bashrc
